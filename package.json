{
  "name": "votatis-src",
  "private": true,
  "version": "0.0.0",
  "description": "Votatis 모노레포 — 선거 무결성 아카이브 (Vite+React · Hono 서버리스 · AWS)",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "pnpm": {
    "comment-supportedArchitectures": "Lambda(Linux x64) 배포 아티팩트용 네이티브 바이너리(@node-rs/argon2-linux-x64-gnu)를 darwin 호스트에서도 함께 받게 한다. apps/api build:lambda 가 동봉.",
    "supportedArchitectures": {
      "os": ["current", "linux"],
      "cpu": ["current", "x64"]
    }
  }
}
